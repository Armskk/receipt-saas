node_modules/
dist/
.next/
*.log
.env
.env.local
.env.*.local
# Per-environment files (.env.stg, .env.production, .env.edge, backend/.env.stg, ...) hold real secrets.
# Only *.example files are committed.
.env.*
!.env.example
!.env.*.example
.DS_Store
coverage/
.turbo/
minio-data/
postgres-data/
# Real receipt photos are personal financial data — never committed (see
# CLAUDE.md's environment matrix and backend/scripts/receipt-accuracy/README.md).
backend/scripts/receipt-accuracy/samples/
