# Keep secrets and build output out of the image. In particular .env.local must not reach
# `next build`: NEXT_PUBLIC_* values come from build args (see Dockerfile), not from files.
.env
.env.*
!.env.example
node_modules
.next
*.log
tsconfig.tsbuildinfo
Dockerfile
.dockerignore
