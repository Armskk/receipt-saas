# Staging. Backend/frontend are reached by their per-environment network aliases
# (see docker-compose.prod.yml), never by the plain service names.
{$STG_APP_DOMAIN} {
	encode zstd gzip
	reverse_proxy stg-frontend:3000
}

{$STG_API_DOMAIN} {
	encode zstd gzip
	reverse_proxy stg-backend:3001
}
